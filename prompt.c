#include "shell.h"

/**
 * shows_prompt - Displays the shell prompt
 *
 * This function displays the shell prompt whenever the shell is initiated.
 * The prompt is typically in the format "kimtopshell$".
 */
void shows_prompt(void)
{
	rkmprint_func("kimtopshell$");
}
