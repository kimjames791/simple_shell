#ifndef SHELL_H
#define SHELL_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

void rkmprint_func(const char *inputmsg);
void shows_prompt(void);
reads_order((order) sizeof(order));
execute_order(order);

#endif /* SHELL_H */
